<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
  <meta http-equiv='X-UA-Compatible' content='IE=edge'>
  <title>OpenActive Test Suite - Validation Errors</title>
  <meta name='viewport' content='width=device-width, initial-scale=1'>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
</head>
<body>

<main role="main">

  <section class="text-center">
    <div class="container">
      <h1>Opportunity Feed Validation Results</h1>
      <p class="lead text-muted">The validator found errors within items in the opportunity feeds. These are listed below with examples of each.</p>
      <!--<p>Note that validation occurs after splits feeds are combined (e.g. after a <code>SessionSeries</code> has been embedded in a <code>ScheduledSession</code>).</p>-->
    </div>
  </section>

  <div class="album py-5 bg-light">
    <div class="container">

      <div class="row">
        {{#each validationErrors }}
        <div class="col-md-12">
          <div class="card shadow-sm" style="margin-bottom: 16px;">
            <div class="card-body">
              <h6 class="card-title">{{{path}}}</h6>
              <p class="card-text">{{{renderMarkdown message}}}</p>
              <div class="d-flex justify-content-between align-items-center">
                <small class="text-muted">{{{occurrences}}} occurrences</small>
              </div>
            </div>
            <ul class="list-group">
              {{#each examples }}
              <li class="list-group-item"><code>{{{ . }}}</code><a href="{{{renderOpenValidatorHref . }}}" target="_blank" class="float-right"><button type="button" class="btn btn-sm btn-outline-secondary">Open Validator</button></a></li>
              {{/each}}
            </ul>
          </div>
        </div>
        {{else}}
        <div class="col-md-12">
          <div class="card shadow-sm" style="margin-bottom: 16px;">
            <div class="card-body">
              <h6 class="card-title">No validation errors found</h6>
              <p class="card-text">There are no validation errors to display</p>
            </div>
          </div>
        </div>
        {{/each}}
      </div>
    </div>
  </div>

</main>

<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"
        integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6"
        crossorigin="anonymous"></script>
</body>
</html>
