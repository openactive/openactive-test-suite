# Summary

{{#each opportunityTypeGroups }}
{{ opportunityTypeName }}
---

  {{#each featureGroups }}
  * {{{ featureName }}} ({{implemented}})
    {{#each loggers}}
    - [{{{ suiteName }}}]({{{ markdownPath }}}): {{{ numFailed }}} failures, {{{ numPassed }}} passes
    {{/each}}
  {{/each}}

{{/each}}
