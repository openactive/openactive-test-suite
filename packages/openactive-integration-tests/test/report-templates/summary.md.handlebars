# Summary

{{#each opportunityTypeGroups }}
{{ opportunityTypeName }}
---

  {{#each featureGroups }}
  * {{{ validationIcon overallStatus }}} {{{ featureName }}} ({{implemented}})
    {{#each loggers}}
    - {{{ validationIcon overallStatus }}} [{{{ suiteName }}}]({{{ markdownPath }}}): ({{{ numFailed }}} failures, {{{ numWarnings }}} warnings, {{{ numSuggestions }}} suggestions, {{{ numPassed }}} passes)
    {{/each}}
  {{/each}}

{{/each}}
