Summary

{{#each opportunityTypeGroups }}
{{# chalk "bold"}}{{ opportunityTypeName }}{{/chalk}}
---

  {{#each featureGroups }}
  * {{{ featureName }}} ({{implemented}})
    {{#each loggers}}
    - {{{ suiteName }}}: {{{ numFailed }}} failures, {{{ numPassed }}} passes: {{{ markdownPath }}}
    {{/each}}
  {{/each}}

{{/each}}
